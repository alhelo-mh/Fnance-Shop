import 'package:flutter/material.dart';


class ProfileSettingScrren extends StatelessWidget {
  const ProfileSettingScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('ProfileSettingScrren')),
    );
  }
}